{% comment %}
  shared.liquid — reserved for plugin-specific CSS overrides if needed.
  The TRMNL design system (plugins.css + plugins.js) is loaded automatically
  by the preview server and the production platform.
{% endcomment %}
